---
import { Icon } from "astro-icon/components";
const iconTools = ["astro", "react", "javascript", "typescript", "tailwindcss"];
---

<section
  class="relative min-h-screen flex flex-col justify-center items-center text-center px-8 bg-gradient-radial from-white via-purple-50 to-purple-200"
>
  <div
    class="absolute inset-0 -z-10"
    style={{
      background:
        "radial-gradient(125% 125% at 50% 90%, #fff 40%, #7d28c8 100%)",
    }}
  >
  </div>
  <!-- Badge accesible -->
  <span
    class="inline-flex items-center justify-center font-medium gap-2 py-2 px-3 sm:px-4 text-xs sm:text-sm rounded-full shadow-lg backdrop-blur-md border bg-white/80 border-gray-200/50 text-gray-900"
    aria-label="Innovamos y creamos soluciones digitales"
  >
    <span class="relative flex h-2 w-2">
      <span
        class="animate-ping absolute inline-flex h-full w-full rounded-full bg-blue-400 opacity-75"
      ></span>
      <span class="relative inline-flex rounded-full h-2 w-2 bg-blue-500"
      ></span>
    </span>
    Innovamos
    <Icon
      name="lightning"
      class="h-3 w-3 text-orange-500"
      is:inline
      aria-hidden="true"
    />
    Creamos
    <Icon name="arrow-right" class="h-3 w-3" is:inline aria-hidden="true" />
  </span>

  <!-- H1 principal optimizado para LCP -->
  <h1
    class="text-4xl tablet:text-6xl desktop:text-7xl font-bold tracking-tight mt-6 mb-1 text-gray-900"
  >
    Hola, soy
    <span class="inline-flex items-center gap-2">
      Alexis
      <Icon
        name="cap"
        class="w-10 h-10 tablet:w-12 tablet:h-12 desktop:w-18 desktop:h-18 text-violet-700"
        is:inline
        aria-hidden="true"
      />
      Chim
    </span>
  </h1>

  <!-- Subtítulo con gradiente ligero -->
  <h2
    class="mb-6 text-2xl sm:text-3xl md:text-4xl font-bold bg-gradient-to-r from-neutral-900 via-slate-500 to-neutral-500 bg-clip-text text-transparent leading-tight"
  >
    Frontend Developer
  </h2>

  <!-- Descripción -->
  <p
    class="text-base sm:text-lg mb-6 sm:mb-8 max-w-lg mx-auto leading-relaxed px-4 sm:px-0 text-gray-600"
  >
    <strong>Transformo ideas</strong> en experiencias atractivas y accesibles, siempre
    pensando en el usuario, el <strong>rendimiento</strong> y los <strong
      >pequeños detalles</strong
    > que hacen la diferencia.
  </p>

  <!-- Botones CTA -->
  <div
    class="flex flex-col justify-center items-center gap-4 w-full px-10 tablet:px-8 tablet:flex-row tablet:w-4/5 laptop:w-1/2"
  >
    <a
      href="#projects"
      class="flex items-center gap-3 p-3 sm:p-4 rounded-xl shadow-lg backdrop-blur-md border w-full sm:w-auto bg-white/70 border-gray-200/30 hover:bg-white/80 focus:outline-none focus:ring-2 focus:ring-violet-400"
    >
      <div class="p-2 rounded-lg bg-violet-100">
        <Icon name="exercism" class="h-6 w-6 text-violet-600" is:inline />
      </div>
      <div class="text-left">
        <h3 class="font-semibold text-sm text-gray-900">
          Mi Zona de commits visuales
        </h3>
        <p class="text-xs text-gray-600">Listas para explorar</p>
      </div>
    </a>

    <a
      href="#services"
      class="flex items-center gap-3 p-3 sm:p-4 rounded-xl shadow-lg backdrop-blur-md border w-full sm:w-auto bg-white/70 border-gray-200/30 hover:bg-white/80 focus:outline-none focus:ring-2 focus:ring-pink-400"
    >
      <div class="p-2 rounded-lg bg-pink-100">
        <Icon name="jabber" class="h-6 w-6 text-pink-600" is:inline />
      </div>
      <div class="text-left">
        <h3 class="font-semibold text-sm text-gray-900">
          Mis soluciones digitales
        </h3>
        <p class="text-xs text-gray-600">A tu disposición</p>
      </div>
    </a>
  </div>

  <!-- Footer con iconos -->
  <footer class="my-7 flex justify-center items-center gap-4">
    {
      iconTools.map((icon) => (
        <Icon
          name={icon}
          class="w-8 h-8"
          is:inline
          aria-label={`Tecnología: ${icon}`}
        />
      ))
    }
  </footer>
</section>
