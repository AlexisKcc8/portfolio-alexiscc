---
import { myContact } from "@data/dataContact";
import { Icon } from "astro-icon/components";
---

<section
  id="hero"
  class="relative min-h-screen flex flex-col justify-center items-center text-center px-6 sm:px-8 bg-gradient-to-t from-white via-purple-50 to-purple-700"
  aria-labelledby="hero-heading"
>
  <!-- Badge accesible -->
  <span
    class="inline-flex items-center justify-center font-medium gap-2 py-2 px-3 sm:px-4 text-xs sm:text-sm rounded-full shadow-md backdrop-blur-md border bg-white/80 border-gray-200/50 text-gray-900"
    aria-label="Innovamos y creamos soluciones digitales"
  >
    <span class="relative flex h-2 w-2">
      <span
        class="animate-ping absolute inline-flex h-full w-full rounded-full bg-blue-400 opacity-75"
      ></span>
      <span class="relative inline-flex rounded-full h-2 w-2 bg-blue-500"
      ></span>
    </span>
    Innovamos ⚡ Creamos →
  </span>

  <!-- H1 principal optimizado para LCP -->
  <h1
    id="hero-heading"
    class="text-5xl sm:text-6xl md:text-7xl font-bold tracking-tight mt-6 mb-1 text-gray-900"
  >
    Hola, soy <span
      class="inline-flex items-center gap-2 font-extrabold mb-3 tracking-tight"
    >
      Alexis
      <Icon
        name="cap"
        class="w-10 h-10 tablet:w-12 tablet:h-12 desktop:w-18 desktop:h-18 text-violet-700"
        is:inline
        aria-hidden="true"
      />
      Chim
    </span>
  </h1>

  <!-- Subtítulo -->
  <h2
    class="mb-6 text-2xl sm:text-3xl md:text-4xl font-bold tracking-tight bg-gradient-to-r from-gray-900 via-gray-800 to-purple-700 bg-clip-text text-transparent leading-tight"
  >
    Frontend Developer
  </h2>

  <!-- Descripción accesible -->
  <p
    class="text-base sm:text-lg mb-6 sm:mb-8 max-w-lg mx-auto leading-relaxed text-gray-600"
  >
    <strong>Transformo ideas</strong> en experiencias atractivas y accesibles, siempre
    pensando en el usuario, el <strong>rendimiento</strong> y los <strong
      >pequeños detalles</strong
    > que hacen la diferencia.
  </p>

  <!-- CTA Buttons -->
  <nav
    aria-label="Llamadas a la acción principales"
    class="flex flex-col justify-center items-center gap-4 w-full px-8 tablet:flex-row tablet:w-4/5 laptop:w-1/2"
  >
    <a
      href="#projects"
      class="flex items-center gap-3 p-3 sm:p-4 rounded-xl shadow-lg backdrop-blur-md border w-full sm:w-auto bg-white/70 border-gray-200/30 hover:bg-white/80 focus:outline-none focus:ring-2 focus:ring-violet-400 transition"
    >
      <div class="p-2 rounded-lg bg-violet-100">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          class="h-6 w-6 text-violet-600"
          fill="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            d="M12 2L15 8H9L12 2ZM2 12L8 15V9L2 12ZM12 22L9 16H15L12 22ZM22 12L16 9V15L22 12Z"
          ></path>
        </svg>
      </div>
      <div class="text-left">
        <h3 class="font-semibold text-sm text-gray-900">
          Mi Zona de commits visuales
        </h3>
        <p class="text-xs text-gray-600">Listas para explorar</p>
      </div>
    </a>

    <a
      href="#services"
      class="flex items-center gap-3 p-3 sm:p-4 rounded-xl shadow-lg backdrop-blur-md border w-full sm:w-auto bg-white/70 border-gray-200/30 hover:bg-white/80 focus:outline-none focus:ring-2 focus:ring-pink-400 transition"
    >
      <div class="p-2 rounded-lg bg-pink-100">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          class="h-6 w-6 text-pink-600"
          fill="currentColor"
          viewBox="0 0 24 24"
        >
          <path d="M2 12L8 15V9L2 12ZM22 12L16 9V15L22 12ZM12 2L9 16H15L12 2Z"
          ></path>
        </svg>
      </div>
      <div class="text-left">
        <h3 class="font-semibold text-sm text-gray-900">
          Mis soluciones digitales
        </h3>
        <p class="text-xs text-gray-600">A tu disposición</p>
      </div>
    </a>
  </nav>

  <!-- Footer con iconos accesibles -->
  <footer
    class="my-7 flex justify-center items-center gap-4"
    aria-label="Enlaces de contacto"
  >
    {
      myContact.map(({ href, bg, textColor, title, icon }) => (
        <a
          href={href}
          class={`p-3 rounded-xl ${bg} ${textColor} hover:opacity-90 focus:outline-none focus:ring-2 focus:ring-offset-2 transition`}
          target="_blank"
          rel="noopener noreferrer"
          aria-label={`Contáctame por ${title}`}
        >
          <Icon name={icon} class="h-6 w-6" is:inline aria-hidden="true" />
        </a>
      ))
    }
  </footer>
</section>
